## ADDED Requirements

### Requirement: Trigger world generation
The system SHALL provide a `simulate` subcommand that initiates the world generation process.

#### Scenario: Running simulate command
- **WHEN** the user runs `app simulate`
- **THEN** the system starts the simulation and reports progress/status
